// 내가 쓴 리뷰 페이지에서 최상단에 위치하는 날짜 검색 컴포넌트입니다.
import React from "react";

const DateSearch = () => {
  return (
    <div>
      내가 쓴 리뷰 페이지에서 최상단에 위치하는 날짜 검색 컴포넌트입니다.
    </div>
  );
};

export default DateSearch;
