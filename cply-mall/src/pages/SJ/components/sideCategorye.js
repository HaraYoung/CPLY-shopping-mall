//  카테고리에서 링크 클릭 후 반환도니는 검색 결과 중 사이드에 해당되는 부분입니다.
import React from "react";

const sideCategorye = () => {
  return (
    <div>
      카테고리에서 링크 클릭 후 반환도니는 검색 결과 중 사이드에 해당되는
      부분입니다.
    </div>
  );
};

export default sideCategorye;
